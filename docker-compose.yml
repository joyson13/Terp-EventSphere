version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: terp-eventsphere-db
    environment:
      POSTGRES_USER: ${DB_USER:-eventsphere}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-eventsphere123}
      POSTGRES_DB: ${DB_NAME:-eventsphere}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-eventsphere}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ADD YOUR APPLICATION SERVICE HERE
  app:
    build: .
    container_name: terp-eventsphere-app
    ports:
      - "5173:5173"
<<<<<<< HEAD
=======
      - "3001:3001"
      - "3002:3002"
      - "3003:3003"
      - "3004:3004"
>>>>>>> feature8-attendee-networking
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${DB_USER:-eventsphere}:${DB_PASSWORD:-eventsphere123}@postgres:5432/${DB_NAME:-eventsphere}
<<<<<<< HEAD
      # Add any other environment variables your app needs
=======
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-eventsphere}
      DB_USER: ${DB_USER:-eventsphere}
      DB_PASSWORD: ${DB_PASSWORD:-eventsphere123}
      NODE_ENV: development
>>>>>>> feature8-attendee-networking
    volumes:
      - .:/app
      - /app/node_modules
    # Remove the line below if you're not using hot-reload
    command: npm run dev

volumes:
  postgres_data: